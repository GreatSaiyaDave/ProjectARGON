# Avatar3D (VRM / GLB)

Drop UniVRM-compatible avatars here as `{vrmId}.vrm`. Set `AvatarAppearance.vrmId` to the file stem.

Until files exist, the game uses the 2D customizer (`../Avatar/`).

See `Assets/Scripts/WRLDZ/AVATAR_3D_INTEGRATION.md`.

